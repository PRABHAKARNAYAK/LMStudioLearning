/**
 * Specifies the available tuning modes for the system.
 *
 * @remarks
 * - `Automatic`: The system tunes itself automatically.
 * - `Manual`: The user manually tunes the system.
 */
export enum TuningMode {
  Automatic,
  Manual,
}
